# Frontend env — Vite reads `VITE_*` vars at build time.
# In Vercel, set this on the project's Environment Variables page (NOT in this file).

VITE_API_URL=http://localhost:4000/api
# Production example: VITE_API_URL=https://broadcast-api.onrender.com/api
